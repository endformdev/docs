<div class="parameter-table" role="region" aria-label="Tool parameters" tabindex="0">
	<slot />
</div>

<style>
	.parameter-table {
		margin-block: 1.5rem;
		overflow-x: auto;
		border: 1px solid var(--sl-color-gray-5);
		border-radius: 0.5rem;
	}

	.parameter-table:focus-visible {
		outline: 2px solid var(--sl-color-accent-high);
		outline-offset: 2px;
	}

	.parameter-table :global(table) {
		display: table;
		width: 100%;
		min-width: 44rem;
		margin: 0;
		overflow: visible;
		border-spacing: 0;
		table-layout: fixed;
	}

	.parameter-table :global(:is(th, td)) {
		padding: 0.75rem 1rem;
		vertical-align: top;
	}

	.parameter-table :global(:is(th, td):first-child) {
		width: 10rem;
		padding-inline: 1rem;
		white-space: nowrap;
	}

	.parameter-table :global(:is(th, td):nth-child(2)) {
		width: 15rem;
	}

	.parameter-table :global(:is(th, td):last-child) {
		padding-inline: 1rem;
	}

	.parameter-table :global(tbody tr:last-child td) {
		border-bottom: 0;
	}
</style>
